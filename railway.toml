[build]
builder = "nixpacks"
installCommand = "pnpm install --no-frozen-lockfile"
buildCommand = "echo 'Skipping build...'"

[deploy]
# Запускаем напрямую через ts-node
startCommand = "pnpm --filter api-server exec ts-node src/index.ts"
healthcheckPath = ""
restartPolicyType = "on_failure"
